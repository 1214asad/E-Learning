<!DOCTYPE html>
<html lang="en">
    <head>
      <title>your blog</title>
      <h1>Your Blogs</h1>
      <a class="hometag" href="/">Home</a>  
    </head>
<style>
  
.blogs{
  font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
  align-items: center;
  align-content: center;
  margin: 10px;
  display: inline-block;
  padding: 5px 10px;
  font-size: 18px;
  text-decoration: none;
  border-radius: 5px;
  box-shadow: 2px 3px 2px 2px rgb(119, 137, 143);
  width: 600px;
}
.heading{
  padding: 2px;
  background-color: aliceblue;
  width: auto;
}

.hometag{
  display: inline-block;
      padding: 12px 24px;
      margin: 10px;
      font-size: 18px;
      font-weight: bold;
      text-decoration: none;
      background-color: #007bff;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    
}
</style>
<body>
<p>If you click on the "blogs" then form-data will be sent to a page called.</p>
<% if(blogs.length > 0){ %>
  <% blogs.forEach(blog =>{ %>
    <% if(user.email == blog.userId){ %>
<a class="blogs"  href="/blogs/ <%= blog._id%> ">
 <h2 class="heading"><%= blog.tittle %></h2>
 <p class="para"><%= blog.snippet %></p>
</a>
    <% } %>
    <% })%>
    <% } %>
</body>
</html>